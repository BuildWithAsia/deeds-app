{
  "name": "deeds-app",
  "version": "0.1.0",
  "private": false,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "prettier --check \"functions/**/*.js\" \"public/**/*.{html,css,js}\"",
    "test": "node --test tests",
    "format": "prettier --write \"functions/**/*.js\" \"public/**/*.{html,css,js}\"",
    "show:deployment": "node scripts/show-deployment.js",
    "demo": "wrangler dev --remote --env demo"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
